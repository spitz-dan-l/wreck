<!-- Generated by `node scripts/confusion_scan.js`. This is the run before
     the fixes of Phase B9; it is what the scanner found on the world as the
     author reported it. -->
# The confusion scan

214 states scanned, 24763 offered commands applied, 484 reachability searches run.

THROW 0 · NO-OP 6 · EMPTY 18 · DEAD END 0 · REPEAT 0 · NOISE 154

## NO-OP (6)

- **`collapse the unmapped`** — it prints "The unmapped rows fold." but nothing is mapped, so it folds every row of the story
  - printed: "The unmapped rows fold."
  - reach it: the acceptance script's first 26 commands (… "let it follow", "draw a vertical line"), then "collapse the unmapped"
- **`collapse the unmapped`** — it prints "The unmapped rows fold." but nothing is mapped, so it folds every row of the story
  - printed: "The unmapped rows fold."
  - reach it: the acceptance script's first 28 commands (… "map the laying of the tinder to the laying of the tinder in the pit", "erase the laying of the tinder"), then "collapse the unmapped"
- **`collapse the unmapped`** — it prints "The unmapped rows fold." but nothing is mapped, so it folds every row of the story
  - printed: "The unmapped rows fold."
  - reach it: the acceptance script's first 73 commands (… "let it follow", "draw a vertical line"), then "collapse the unmapped"
- **`collapse the unmapped`** — it prints "The unmapped rows fold." but nothing is mapped, so it folds every row of the story
  - printed: "The unmapped rows fold."
  - reach it: the acceptance script's first 121 commands (… "stop at the rivers", "draw a vertical line"), then "collapse the unmapped"
- **`collapse the unmapped`** — it prints "The unmapped rows fold." but nothing is mapped, so it folds every row of the story
  - printed: "The unmapped rows fold."
  - reach it: the acceptance script's first 162 commands (… "pass", "draw a vertical line"), then "collapse the unmapped"
- **`collapse the unmapped`** — it prints "The unmapped rows fold." but nothing is mapped, so it folds every row of the story
  - printed: "The unmapped rows fold."
  - reach it: the acceptance script's first 163 commands (… "draw a vertical line", "say that the Voice of Fire is contained in just two lines"), then "collapse the unmapped"

## EMPTY (18)

- **`speak as the friends`** — it changes the scene and prints no consequence at all
  - reach it: the acceptance script's first 10 commands (… "say that the Voice of Fire is contained in this one", "pick up the chalk"), then "speak as the friends"
- **`pick up the chalk`** — it changes the scene and prints no consequence at all
  - reach it: the acceptance script's first 49 commands (… "listen", "say that it is a sad story"), then "pick up the chalk"
- **`speak as the family`** — it changes the scene and prints no consequence at all
  - reach it: the acceptance script's first 50 commands (… "say that it is a sad story", "pick up the chalk"), then "speak as the family"
- **`pick up the chalk`** — it changes the scene and prints no consequence at all
  - reach it: the acceptance script's first 100 commands (… "remember the house in the woods", "listen"), then "pick up the chalk"
- **`pick up the chalk`** — it changes the scene and prints no consequence at all
  - reach it: the acceptance script's first 134 commands (… "put down the chalk", "listen"), then "pick up the chalk"
- **`draw a vertical line`** — it changes the scene and prints no consequence at all
  - reach it: the acceptance script's first 161 commands (… "speak as time", "pass"), then "draw a vertical line"
- **`try the Pillaging on the house in the woods`** — it changes the scene and prints no consequence at all
  - reach it: the acceptance script's first 200 commands (… "remember the wise man's story", "say Ok, I guess"), then "try the Pillaging on the house in the woods"
- **`try the Pillaging on the house in the woods`** — it changes the scene and prints no consequence at all
  - reach it: the acceptance script's first 201 commands (… "say Ok, I guess", "remember the wise man's story"), then "try the Pillaging on the house in the woods"
- **`try the Pillaging on the house in the woods`** — it changes the scene and prints no consequence at all
  - reach it: the acceptance script's first 202 commands (… "remember the wise man's story", "remember the saying of Ok, I guess"), then "try the Pillaging on the house in the woods"
- **`try the Pillaging on the house in the woods`** — it changes the scene and prints no consequence at all
  - reach it: the acceptance script's first 203 commands (… "remember the saying of Ok, I guess", "remember the second listening"), then "try the Pillaging on the house in the woods"
- **`try the Pillaging on the house in the woods`** — it changes the scene and prints no consequence at all
  - reach it: the acceptance script's first 204 commands (… "remember the second listening", "remember the ember"), then "try the Pillaging on the house in the woods"
- **`try the Pillaging on the house in the woods`** — it changes the scene and prints no consequence at all
  - reach it: the acceptance script's first 205 commands (… "remember the ember", "remember the lighting of the pyre"), then "try the Pillaging on the house in the woods"
- **`put down the chalk`** — it changes the scene and prints no consequence at all
  - reach it: the acceptance script's first 206 commands (… "remember the lighting of the pyre", "try the Pillaging on the house in the woods"), then "put down the chalk"
- **`put down the chalk`** — it changes the scene and prints no consequence at all
  - reach it: the acceptance script's first 207 commands (… "try the Pillaging on the house in the woods", "map the living in their home to the moving in"), then "put down the chalk"
- **`put down the chalk`** — it changes the scene and prints no consequence at all
  - reach it: the acceptance script's first 208 commands (… "map the living in their home to the moving in", "map the entering of their home to the lighting of the rag"), then "put down the chalk"
- **`put down the chalk`** — it changes the scene and prints no consequence at all
  - reach it: the acceptance script's first 209 commands (… "map the entering of their home to the lighting of the rag", "map the taking of things from their home to the hurling of the rag onto the roof"), then "put down the chalk"
- **`put down the chalk`** — it changes the scene and prints no consequence at all
  - reach it: the acceptance script's first 210 commands (… "map the taking of things from their home to the hurling of the rag onto the roof", "map the entering of their home to the scattering"), then "put down the chalk"
- **`put down the chalk`** — it changes the scene and prints no consequence at all
  - reach it: the acceptance script's first 211 commands (… "map the entering of their home to the scattering", "remember the tinder"), then "put down the chalk"

## NOISE (154)

- **`(the option list after 18 commands)`** — 7 per-event expand/collapse options: "collapse the traveling to the woods" … "collapse the lighting of a match"
  - reach it: the acceptance script's first 18 commands (… "stack the logs over the kindling", "light a match"), the option list there
- **`(the option list after 19 commands)`** — 8 per-event expand/collapse options: "collapse the traveling to the woods" … "collapse the touching of the flame to the tinder"
  - reach it: the acceptance script's first 19 commands (… "light a match", "touch the flame to the tinder"), the option list there
- **`(the option list after 20 commands)`** — 8 per-event expand/collapse options: "collapse the traveling to the woods" … "collapse the touching of the flame to the tinder"
  - reach it: the acceptance script's first 20 commands (… "touch the flame to the tinder", "let it follow"), the option list there
- **`(the option list after 21 commands)`** — 9 per-event expand/collapse options: "collapse the traveling to the woods" … "collapse the first singing"
  - reach it: the acceptance script's first 21 commands (… "let it follow", "sing"), the option list there
- **`(the option list after 22 commands)`** — 10 per-event expand/collapse options: "collapse the traveling to the woods" … "collapse the adding of logs to the fire"
  - reach it: the acceptance script's first 22 commands (… "sing", "add logs to the fire"), the option list there
- **`(the option list after 23 commands)`** — 11 per-event expand/collapse options: "collapse the traveling to the woods" … "collapse the second singing"
  - reach it: the acceptance script's first 23 commands (… "add logs to the fire", "sing"), the option list there
- **`(the option list after 24 commands)`** — 12 per-event expand/collapse options: "collapse the traveling to the woods" … "collapse the sleeping in tents"
  - reach it: the acceptance script's first 24 commands (… "sing", "sleep in tents"), the option list there
- **`(the option list after 25 commands)`** — 12 per-event expand/collapse options: "collapse the traveling to the woods" … "collapse the sleeping in tents"
  - reach it: the acceptance script's first 25 commands (… "sleep in tents", "let it follow"), the option list there
- **`(the option list after 26 commands)`** — 12 per-event expand/collapse options: "collapse the traveling to the woods" … "collapse the sleeping in tents"
  - reach it: the acceptance script's first 26 commands (… "let it follow", "draw a vertical line"), the option list there
- **`(the option list after 27 commands)`** — 12 per-event expand/collapse options: "collapse the traveling to the woods" … "collapse the sleeping in tents"
  - reach it: the acceptance script's first 27 commands (… "draw a vertical line", "map the laying of the tinder to the laying of the tinder in the pit"), the option list there
- **`(the option list after 28 commands)`** — 12 per-event expand/collapse options: "collapse the traveling to the woods" … "collapse the sleeping in tents"
  - reach it: the acceptance script's first 28 commands (… "map the laying of the tinder to the laying of the tinder in the pit", "erase the laying of the tinder"), the option list there
- **`(the option list after 29 commands)`** — 12 per-event expand/collapse options: "collapse the traveling to the woods" … "collapse the sleeping in tents"
  - reach it: the acceptance script's first 29 commands (… "erase the laying of the tinder", "map the laying of the tinder to the laying of the tinder in the pit"), the option list there
- **`(the option list after 30 commands)`** — 12 per-event expand/collapse options: "collapse the traveling to the woods" … "collapse the sleeping in tents"
  - reach it: the acceptance script's first 30 commands (… "map the laying of the tinder to the laying of the tinder in the pit", "map the laying of the kindling to the piling of the kindling"), the option list there
- **`(the option list after 31 commands)`** — 12 per-event expand/collapse options: "collapse the traveling to the woods" … "collapse the sleeping in tents"
  - reach it: the acceptance script's first 31 commands (… "map the laying of the kindling to the piling of the kindling", "map the stacking of the firewood to the stacking of the logs"), the option list there
- **`(the option list after 32 commands)`** — 12 per-event expand/collapse options: "collapse the traveling to the woods" … "collapse the sleeping in tents"
  - reach it: the acceptance script's first 32 commands (… "map the stacking of the firewood to the stacking of the logs", "map the sparking of the tinder to the touching of the flame to the tinder"), the option list there
- **`(the option list after 33 commands)`** — 12 per-event expand/collapse options: "collapse the traveling to the woods" … "collapse the sleeping in tents"
  - reach it: the acceptance script's first 33 commands (… "map the sparking of the tinder to the touching of the flame to the tinder", "map the spreading of the ember to the touching of the flame to the tinder"), the option list there
- **`(the option list after 34 commands)`** — 12 per-event expand/collapse options: "collapse the traveling to the woods" … "collapse the sleeping in tents"
  - reach it: the acceptance script's first 34 commands (… "map the spreading of the ember to the touching of the flame to the tinder", "map the spreading of the flame to the touching of the flame to the tinder"), the option list there
- **`(the option list after 35 commands)`** — 12 per-event expand/collapse options: "collapse the traveling to the woods" … "collapse the sleeping in tents"
  - reach it: the acceptance script's first 35 commands (… "map the spreading of the flame to the touching of the flame to the tinder", "map the consumption of all to the adding of logs to the fire"), the option list there
- **`(the option list after 36 commands)`** — 12 per-event expand/collapse options: "collapse the traveling to the woods" … "collapse the sleeping in tents"
  - reach it: the acceptance script's first 36 commands (… "map the consumption of all to the adding of logs to the fire", "map the ash left behind to the sleeping in tents"), the option list there
- **`(the option list after 37 commands)`** — 12 per-event expand/collapse options: "collapse the traveling to the woods" … "collapse the sleeping in tents"
  - reach it: the acceptance script's first 37 commands (… "map the ash left behind to the sleeping in tents", "apply the Voice of Fire"), the option list there
- **`(the option list after 38 commands)`** — 12 per-event expand/collapse options: "collapse the traveling to the woods" … "collapse the sleeping in tents"
  - reach it: the acceptance script's first 38 commands (… "apply the Voice of Fire", "set aside the mapping"), the option list there
- **`(the option list after 39 commands)`** — 12 per-event expand/collapse options: "collapse the traveling to the woods" … "collapse the sleeping in tents"
  - reach it: the acceptance script's first 39 commands (… "set aside the mapping", "resume the mapping"), the option list there
- **`(the option list after 58 commands)`** — 7 per-event expand/collapse options: "collapse the packing" … "collapse the building of the foundation"
  - reach it: the acceptance script's first 58 commands (… "dig a hole", "build the foundation"), the option list there
- **`(the option list after 59 commands)`** — 8 per-event expand/collapse options: "collapse the packing" … "collapse the raising of the frame"
  - reach it: the acceptance script's first 59 commands (… "build the foundation", "raise the frame"), the option list there
- **`(the option list after 60 commands)`** — 9 per-event expand/collapse options: "collapse the packing" … "collapse the laying of walls and a roof"
  - reach it: the acceptance script's first 60 commands (… "raise the frame", "lay walls and a roof"), the option list there
- **`(the option list after 61 commands)`** — 10 per-event expand/collapse options: "collapse the packing" … "collapse the moving in"
  - reach it: the acceptance script's first 61 commands (… "lay walls and a roof", "move in"), the option list there
- **`(the option list after 62 commands)`** — 10 per-event expand/collapse options: "collapse the packing" … "collapse the moving in"
  - reach it: the acceptance script's first 62 commands (… "move in", "let it follow"), the option list there
- **`(the option list after 63 commands)`** — 10 per-event expand/collapse options: "collapse the packing" … "collapse the moving in"
  - reach it: the acceptance script's first 63 commands (… "let it follow", "let it follow"), the option list there
- **`(the option list after 64 commands)`** — 10 per-event expand/collapse options: "collapse the packing" … "collapse the moving in"
  - reach it: the acceptance script's first 64 commands (… "let it follow", "ask what the right thing to do is"), the option list there
- **`(the option list after 65 commands)`** — 10 per-event expand/collapse options: "collapse the packing" … "collapse the moving in"
  - reach it: the acceptance script's first 65 commands (… "ask what the right thing to do is", "speak as the children"), the option list there
- **`(the option list after 66 commands)`** — 11 per-event expand/collapse options: "collapse the packing" … "collapse the lighting of the rag"
  - reach it: the acceptance script's first 66 commands (… "speak as the children", "light the rag"), the option list there
- **`(the option list after 67 commands)`** — 12 per-event expand/collapse options: "collapse the packing" … "collapse the hurling of the rag onto the roof"
  - reach it: the acceptance script's first 67 commands (… "light the rag", "hurl it onto the roof"), the option list there
- **`(the option list after 68 commands)`** — 13 per-event expand/collapse options: "collapse the packing" … "collapse the scattering"
  - reach it: the acceptance script's first 68 commands (… "hurl it onto the roof", "scatter"), the option list there
- **`(the option list after 69 commands)`** — 13 per-event expand/collapse options: "collapse the packing" … "collapse the scattering"
  - reach it: the acceptance script's first 69 commands (… "scatter", "let it follow"), the option list there
- **`(the option list after 70 commands)`** — 13 per-event expand/collapse options: "collapse the packing" … "collapse the scattering"
  - reach it: the acceptance script's first 70 commands (… "let it follow", "let it follow"), the option list there
- **`(the option list after 71 commands)`** — 13 per-event expand/collapse options: "collapse the packing" … "collapse the scattering"
  - reach it: the acceptance script's first 71 commands (… "let it follow", "let it follow"), the option list there
- **`(the option list after 72 commands)`** — 13 per-event expand/collapse options: "collapse the packing" … "collapse the scattering"
  - reach it: the acceptance script's first 72 commands (… "let it follow", "let it follow"), the option list there
- **`(the option list after 73 commands)`** — 13 per-event expand/collapse options: "collapse the packing" … "collapse the scattering"
  - reach it: the acceptance script's first 73 commands (… "let it follow", "draw a vertical line"), the option list there
- **`(the option list after 74 commands)`** — 13 per-event expand/collapse options: "collapse the packing" … "collapse the scattering"
  - reach it: the acceptance script's first 74 commands (… "draw a vertical line", "map the laying of the tinder to the laying of walls and a roof"), the option list there
- **`(the option list after 75 commands)`** — 13 per-event expand/collapse options: "collapse the packing" … "collapse the scattering"
  - reach it: the acceptance script's first 75 commands (… "map the laying of the tinder to the laying of walls and a roof", "map the laying of the tinder to the lighting of the rag"), the option list there
- **`(the option list after 76 commands)`** — 13 per-event expand/collapse options: "collapse the packing" … "collapse the scattering"
  - reach it: the acceptance script's first 76 commands (… "map the laying of the tinder to the lighting of the rag", "map the laying of the kindling to the laying of walls and a roof"), the option list there
- **`(the option list after 77 commands)`** — 13 per-event expand/collapse options: "collapse the packing" … "collapse the scattering"
  - reach it: the acceptance script's first 77 commands (… "map the laying of the kindling to the laying of walls and a roof", "map the stacking of the firewood to the raising of the frame"), the option list there
- **`(the option list after 78 commands)`** — 13 per-event expand/collapse options: "collapse the packing" … "collapse the scattering"
  - reach it: the acceptance script's first 78 commands (… "map the stacking of the firewood to the raising of the frame", "map the sparking of the tinder to the hurling of the rag onto the roof"), the option list there
- **`(the option list after 79 commands)`** — 13 per-event expand/collapse options: "collapse the packing" … "collapse the scattering"
  - reach it: the acceptance script's first 79 commands (… "map the sparking of the tinder to the hurling of the rag onto the roof", "map the spreading of the ember to the scattering"), the option list there
- **`(the option list after 80 commands)`** — 13 per-event expand/collapse options: "collapse the packing" … "collapse the scattering"
  - reach it: the acceptance script's first 80 commands (… "map the spreading of the ember to the scattering", "map the spreading of the flame to the scattering"), the option list there
- **`(the option list after 81 commands)`** — 13 per-event expand/collapse options: "collapse the packing" … "collapse the scattering"
  - reach it: the acceptance script's first 81 commands (… "map the spreading of the flame to the scattering", "map the consumption of all to the scattering"), the option list there
- **`(the option list after 82 commands)`** — 13 per-event expand/collapse options: "collapse the packing" … "collapse the scattering"
  - reach it: the acceptance script's first 82 commands (… "map the consumption of all to the scattering", "map the ash left behind to the scattering"), the option list there
- **`(the option list after 83 commands)`** — 13 per-event expand/collapse options: "collapse the packing" … "collapse the scattering"
  - reach it: the acceptance script's first 83 commands (… "map the ash left behind to the scattering", "apply the Voice of Fire"), the option list there
- **`(the option list after 84 commands)`** — 13 per-event expand/collapse options: "collapse the packing" … "collapse the scattering"
  - reach it: the acceptance script's first 84 commands (… "apply the Voice of Fire", "remember the lighting of the rag"), the option list there
- **`(the option list after 85 commands)`** — 13 per-event expand/collapse options: "collapse the packing" … "collapse the scattering"
  - reach it: the acceptance script's first 85 commands (… "remember the lighting of the rag", "remember the scattering"), the option list there
- **`(the option list after 86 commands)`** — 13 per-event expand/collapse options: "collapse the packing" … "collapse the scattering"
  - reach it: the acceptance script's first 86 commands (… "remember the scattering", "set aside the mapping"), the option list there
- **`(the option list after 87 commands)`** — 13 per-event expand/collapse options: "collapse the packing" … "collapse the scattering"
  - reach it: the acceptance script's first 87 commands (… "set aside the mapping", "erase the sparking of the tinder"), the option list there
- **`(the option list after 88 commands)`** — 13 per-event expand/collapse options: "collapse the packing" … "collapse the scattering"
  - reach it: the acceptance script's first 88 commands (… "erase the sparking of the tinder", "collapse the unmapped"), the option list there
- **`(the option list after 89 commands)`** — 13 per-event expand/collapse options: "collapse the packing" … "collapse the scattering"
  - reach it: the acceptance script's first 89 commands (… "collapse the unmapped", "map the stacking of the firewood to the building of the foundation"), the option list there
- **`(the option list after 90 commands)`** — 13 per-event expand/collapse options: "collapse the packing" … "collapse the scattering"
  - reach it: the acceptance script's first 90 commands (… "map the stacking of the firewood to the building of the foundation", "map the laying of the kindling to the raising of the frame"), the option list there
- **`(the option list after 91 commands)`** — 13 per-event expand/collapse options: "collapse the packing" … "collapse the scattering"
  - reach it: the acceptance script's first 91 commands (… "map the laying of the kindling to the raising of the frame", "map the laying of the tinder to the laying of walls and a roof"), the option list there
- **`(the option list after 92 commands)`** — 13 per-event expand/collapse options: "collapse the packing" … "collapse the scattering"
  - reach it: the acceptance script's first 92 commands (… "map the laying of the tinder to the laying of walls and a roof", "map the sparking of the tinder to the lighting of the rag"), the option list there
- **`(the option list after 93 commands)`** — 13 per-event expand/collapse options: "collapse the packing" … "collapse the scattering"
  - reach it: the acceptance script's first 93 commands (… "map the sparking of the tinder to the lighting of the rag", "expand the unmapped"), the option list there
- **`(the option list after 94 commands)`** — 13 per-event expand/collapse options: "collapse the packing" … "collapse the scattering"
  - reach it: the acceptance script's first 94 commands (… "expand the unmapped", "apply the Voice of Fire"), the option list there
- **`(the option list after 95 commands)`** — 13 per-event expand/collapse options: "collapse the packing" … "collapse the scattering"
  - reach it: the acceptance script's first 95 commands (… "apply the Voice of Fire", "remember the tinder"), the option list there
- **`(the option list after 96 commands)`** — 13 per-event expand/collapse options: "collapse the packing" … "collapse the scattering"
  - reach it: the acceptance script's first 96 commands (… "remember the tinder", "object that there is no clear tinder"), the option list there
- **`(the option list after 97 commands)`** — 13 per-event expand/collapse options: "collapse the packing" … "collapse the scattering"
  - reach it: the acceptance script's first 97 commands (… "object that there is no clear tinder", "say that it knows nothing of the morality of the burning either"), the option list there
- **`(the option list after 112 commands)`** — 7 per-event expand/collapse options: "collapse the taking root" … "expand the house in the woods"
  - reach it: the acceptance script's first 112 commands (… "speak as time", "pass"), the option list there
- **`(the option list after 113 commands)`** — 7 per-event expand/collapse options: "collapse the taking root" … "expand the house in the woods"
  - reach it: the acceptance script's first 113 commands (… "pass", "speak as the weather"), the option list there
- **`(the option list after 114 commands)`** — 8 per-event expand/collapse options: "collapse the taking root" … "expand the house in the woods"
  - reach it: the acceptance script's first 114 commands (… "speak as the weather", "turn dry and hot"), the option list there
- **`(the option list after 115 commands)`** — 9 per-event expand/collapse options: "collapse the taking root" … "expand the house in the woods"
  - reach it: the acceptance script's first 115 commands (… "turn dry and hot", "bring a thunderstorm"), the option list there
- **`(the option list after 116 commands)`** — 9 per-event expand/collapse options: "collapse the taking root" … "expand the house in the woods"
  - reach it: the acceptance script's first 116 commands (… "bring a thunderstorm", "speak as the fire"), the option list there
- **`(the option list after 117 commands)`** — 10 per-event expand/collapse options: "collapse the taking root" … "expand the house in the woods"
  - reach it: the acceptance script's first 117 commands (… "speak as the fire", "spread to the dead brush"), the option list there
- **`(the option list after 118 commands)`** — 11 per-event expand/collapse options: "collapse the taking root" … "expand the house in the woods"
  - reach it: the acceptance script's first 118 commands (… "spread to the dead brush", "burn in a growing circle"), the option list there
- **`(the option list after 119 commands)`** — 12 per-event expand/collapse options: "collapse the taking root" … "expand the house in the woods"
  - reach it: the acceptance script's first 119 commands (… "burn in a growing circle", "consume the forest"), the option list there
- **`(the option list after 120 commands)`** — 13 per-event expand/collapse options: "collapse the taking root" … "expand the house in the woods"
  - reach it: the acceptance script's first 120 commands (… "consume the forest", "stop at the rivers"), the option list there
- **`(the option list after 121 commands)`** — 13 per-event expand/collapse options: "collapse the taking root" … "expand the house in the woods"
  - reach it: the acceptance script's first 121 commands (… "stop at the rivers", "draw a vertical line"), the option list there
- **`(the option list after 122 commands)`** — 13 per-event expand/collapse options: "collapse the taking root" … "expand the house in the woods"
  - reach it: the acceptance script's first 122 commands (… "draw a vertical line", "map the laying of the tinder to the turning dry and hot"), the option list there
- **`(the option list after 123 commands)`** — 13 per-event expand/collapse options: "collapse the taking root" … "expand the house in the woods"
  - reach it: the acceptance script's first 123 commands (… "map the laying of the tinder to the turning dry and hot", "map the laying of the kindling to the turning dry and hot"), the option list there
- **`(the option list after 124 commands)`** — 13 per-event expand/collapse options: "collapse the taking root" … "expand the house in the woods"
  - reach it: the acceptance script's first 124 commands (… "map the laying of the kindling to the turning dry and hot", "map the stacking of the firewood to the spreading of the forest"), the option list there
- **`(the option list after 125 commands)`** — 13 per-event expand/collapse options: "collapse the taking root" … "expand the house in the woods"
  - reach it: the acceptance script's first 125 commands (… "map the stacking of the firewood to the spreading of the forest", "map the sparking of the tinder to the bringing of a thunderstorm"), the option list there
- **`(the option list after 126 commands)`** — 13 per-event expand/collapse options: "collapse the taking root" … "expand the house in the woods"
  - reach it: the acceptance script's first 126 commands (… "map the sparking of the tinder to the bringing of a thunderstorm", "map the spreading of the ember to the spreading to the dead brush"), the option list there
- **`(the option list after 127 commands)`** — 13 per-event expand/collapse options: "collapse the taking root" … "expand the house in the woods"
  - reach it: the acceptance script's first 127 commands (… "map the spreading of the ember to the spreading to the dead brush", "map the spreading of the flame to the spreading to the dead brush"), the option list there
- **`(the option list after 128 commands)`** — 13 per-event expand/collapse options: "collapse the taking root" … "expand the house in the woods"
  - reach it: the acceptance script's first 128 commands (… "map the spreading of the flame to the spreading to the dead brush", "map the consumption of all to the burning in a growing circle"), the option list there
- **`(the option list after 129 commands)`** — 13 per-event expand/collapse options: "collapse the taking root" … "expand the house in the woods"
  - reach it: the acceptance script's first 129 commands (… "map the consumption of all to the burning in a growing circle", "map the ash left behind to the stopping at the rivers"), the option list there
- **`(the option list after 130 commands)`** — 13 per-event expand/collapse options: "collapse the taking root" … "expand the house in the woods"
  - reach it: the acceptance script's first 130 commands (… "map the ash left behind to the stopping at the rivers", "apply the Voice of Fire"), the option list there
- **`(the option list after 131 commands)`** — 13 per-event expand/collapse options: "collapse the taking root" … "expand the house in the woods"
  - reach it: the acceptance script's first 131 commands (… "apply the Voice of Fire", "set aside the mapping"), the option list there
- **`(the option list after 132 commands)`** — 13 per-event expand/collapse options: "collapse the taking root" … "expand the house in the woods"
  - reach it: the acceptance script's first 132 commands (… "set aside the mapping", "apply the Voice of Fire"), the option list there
- **`(the option list after 143 commands)`** — 7 per-event expand/collapse options: "collapse the being born" … "expand the forest fire"
  - reach it: the acceptance script's first 143 commands (… "speak as the followers", "grow in number"), the option list there
- **`(the option list after 144 commands)`** — 7 per-event expand/collapse options: "collapse the being born" … "expand the forest fire"
  - reach it: the acceptance script's first 144 commands (… "grow in number", "speak as the man"), the option list there
- **`(the option list after 145 commands)`** — 8 per-event expand/collapse options: "collapse the being born" … "expand the forest fire"
  - reach it: the acceptance script's first 145 commands (… "speak as the man", "give speeches"), the option list there
- **`(the option list after 146 commands)`** — 8 per-event expand/collapse options: "collapse the being born" … "expand the forest fire"
  - reach it: the acceptance script's first 146 commands (… "give speeches", "speak as the followers"), the option list there
- **`(the option list after 147 commands)`** — 9 per-event expand/collapse options: "collapse the being born" … "expand the forest fire"
  - reach it: the acceptance script's first 147 commands (… "speak as the followers", "write down his teachings"), the option list there
- **`(the option list after 148 commands)`** — 9 per-event expand/collapse options: "collapse the being born" … "expand the forest fire"
  - reach it: the acceptance script's first 148 commands (… "write down his teachings", "speak as the man"), the option list there
- **`(the option list after 149 commands)`** — 10 per-event expand/collapse options: "collapse the being born" … "expand the forest fire"
  - reach it: the acceptance script's first 149 commands (… "speak as the man", "die unexpectedly"), the option list there
- **`(the option list after 150 commands)`** — 10 per-event expand/collapse options: "collapse the being born" … "expand the forest fire"
  - reach it: the acceptance script's first 150 commands (… "die unexpectedly", "speak as the closest followers"), the option list there
- **`(the option list after 151 commands)`** — 11 per-event expand/collapse options: "collapse the being born" … "expand the forest fire"
  - reach it: the acceptance script's first 151 commands (… "speak as the closest followers", "construct a pyre and lay his body on it"), the option list there
- **`(the option list after 152 commands)`** — 11 per-event expand/collapse options: "collapse the being born" … "expand the forest fire"
  - reach it: the acceptance script's first 152 commands (… "construct a pyre and lay his body on it", "speak as the followers"), the option list there
- **`(the option list after 153 commands)`** — 12 per-event expand/collapse options: "collapse the being born" … "expand the forest fire"
  - reach it: the acceptance script's first 153 commands (… "speak as the followers", "attend the funeral"), the option list there
- **`(the option list after 154 commands)`** — 12 per-event expand/collapse options: "collapse the being born" … "expand the forest fire"
  - reach it: the acceptance script's first 154 commands (… "attend the funeral", "speak as the closest followers"), the option list there
- **`(the option list after 155 commands)`** — 13 per-event expand/collapse options: "collapse the being born" … "expand the forest fire"
  - reach it: the acceptance script's first 155 commands (… "speak as the closest followers", "light the pyre"), the option list there
- **`(the option list after 156 commands)`** — 14 per-event expand/collapse options: "collapse the being born" … "expand the forest fire"
  - reach it: the acceptance script's first 156 commands (… "light the pyre", "adjust and embellish his words"), the option list there
- **`(the option list after 157 commands)`** — 14 per-event expand/collapse options: "collapse the being born" … "expand the forest fire"
  - reach it: the acceptance script's first 157 commands (… "adjust and embellish his words", "speak as the books"), the option list there
- **`(the option list after 158 commands)`** — 15 per-event expand/collapse options: "collapse the being born" … "expand the forest fire"
  - reach it: the acceptance script's first 158 commands (… "speak as the books", "spread across the land"), the option list there
- **`(the option list after 159 commands)`** — 16 per-event expand/collapse options: "collapse the being born" … "expand the forest fire"
  - reach it: the acceptance script's first 159 commands (… "spread across the land", "be read and repeated and reprinted"), the option list there
- **`(the option list after 160 commands)`** — 16 per-event expand/collapse options: "collapse the being born" … "expand the forest fire"
  - reach it: the acceptance script's first 160 commands (… "be read and repeated and reprinted", "speak as time"), the option list there
- **`(the option list after 161 commands)`** — 16 per-event expand/collapse options: "collapse the being born" … "expand the forest fire"
  - reach it: the acceptance script's first 161 commands (… "speak as time", "pass"), the option list there
- **`(the option list after 162 commands)`** — 16 per-event expand/collapse options: "collapse the being born" … "expand the forest fire"
  - reach it: the acceptance script's first 162 commands (… "pass", "draw a vertical line"), the option list there
- **`(the option list after 163 commands)`** — 16 per-event expand/collapse options: "collapse the being born" … "expand the forest fire"
  - reach it: the acceptance script's first 163 commands (… "draw a vertical line", "say that the Voice of Fire is contained in just two lines"), the option list there
- **`(the option list after 164 commands)`** — 16 per-event expand/collapse options: "collapse the being born" … "expand the forest fire"
  - reach it: the acceptance script's first 164 commands (… "say that the Voice of Fire is contained in just two lines", "map the laying of the tinder to the constructing of the pyre"), the option list there
- **`(the option list after 165 commands)`** — 16 per-event expand/collapse options: "collapse the being born" … "expand the forest fire"
  - reach it: the acceptance script's first 165 commands (… "map the laying of the tinder to the constructing of the pyre", "map the laying of the kindling to the constructing of the pyre"), the option list there
- **`(the option list after 166 commands)`** — 16 per-event expand/collapse options: "collapse the being born" … "expand the forest fire"
  - reach it: the acceptance script's first 166 commands (… "map the laying of the kindling to the constructing of the pyre", "map the stacking of the firewood to the constructing of the pyre"), the option list there
- **`(the option list after 167 commands)`** — 16 per-event expand/collapse options: "collapse the being born" … "expand the forest fire"
  - reach it: the acceptance script's first 167 commands (… "map the stacking of the firewood to the constructing of the pyre", "map the sparking of the tinder to the lighting of the pyre"), the option list there
- **`(the option list after 168 commands)`** — 16 per-event expand/collapse options: "collapse the being born" … "expand the forest fire"
  - reach it: the acceptance script's first 168 commands (… "map the sparking of the tinder to the lighting of the pyre", "map the spreading of the ember to the lighting of the pyre"), the option list there
- **`(the option list after 169 commands)`** — 16 per-event expand/collapse options: "collapse the being born" … "expand the forest fire"
  - reach it: the acceptance script's first 169 commands (… "map the spreading of the ember to the lighting of the pyre", "map the spreading of the flame to the lighting of the pyre"), the option list there
- **`(the option list after 170 commands)`** — 16 per-event expand/collapse options: "collapse the being born" … "expand the forest fire"
  - reach it: the acceptance script's first 170 commands (… "map the spreading of the flame to the lighting of the pyre", "map the consumption of all to the lighting of the pyre"), the option list there
- **`(the option list after 171 commands)`** — 16 per-event expand/collapse options: "collapse the being born" … "expand the forest fire"
  - reach it: the acceptance script's first 171 commands (… "map the consumption of all to the lighting of the pyre", "map the ash left behind to the lighting of the pyre"), the option list there
- **`(the option list after 172 commands)`** — 16 per-event expand/collapse options: "collapse the being born" … "expand the forest fire"
  - reach it: the acceptance script's first 172 commands (… "map the ash left behind to the lighting of the pyre", "apply the Voice of Fire"), the option list there
- **`(the option list after 173 commands)`** — 16 per-event expand/collapse options: "collapse the being born" … "expand the forest fire"
  - reach it: the acceptance script's first 173 commands (… "apply the Voice of Fire", "remember the two lines"), the option list there
- **`(the option list after 174 commands)`** — 16 per-event expand/collapse options: "collapse the being born" … "expand the forest fire"
  - reach it: the acceptance script's first 174 commands (… "remember the two lines", "collapse the unmapped"), the option list there
- **`(the option list after 175 commands)`** — 16 per-event expand/collapse options: "collapse the being born" … "expand the forest fire"
  - reach it: the acceptance script's first 175 commands (… "collapse the unmapped", "expand the unmapped"), the option list there
- **`(the option list after 176 commands)`** — 16 per-event expand/collapse options: "collapse the being born" … "expand the forest fire"
  - reach it: the acceptance script's first 176 commands (… "expand the unmapped", "ask what she means"), the option list there
- **`(the option list after 177 commands)`** — 16 per-event expand/collapse options: "collapse the being born" … "expand the forest fire"
  - reach it: the acceptance script's first 177 commands (… "ask what she means", "set aside the first solution"), the option list there
- **`(the option list after 178 commands)`** — 16 per-event expand/collapse options: "collapse the being born" … "expand the forest fire"
  - reach it: the acceptance script's first 178 commands (… "set aside the first solution", "map the laying of the tinder to the growing up and acquiring of wisdom"), the option list there
- **`(the option list after 179 commands)`** — 16 per-event expand/collapse options: "collapse the being born" … "expand the forest fire"
  - reach it: the acceptance script's first 179 commands (… "map the laying of the tinder to the growing up and acquiring of wisdom", "map the laying of the kindling to the gaining of a small circle"), the option list there
- **`(the option list after 180 commands)`** — 16 per-event expand/collapse options: "collapse the being born" … "expand the forest fire"
  - reach it: the acceptance script's first 180 commands (… "map the laying of the kindling to the gaining of a small circle", "map the stacking of the firewood to the growing in number"), the option list there
- **`(the option list after 181 commands)`** — 16 per-event expand/collapse options: "collapse the being born" … "expand the forest fire"
  - reach it: the acceptance script's first 181 commands (… "map the stacking of the firewood to the growing in number", "map the sparking of the tinder to the dying unexpectedly"), the option list there
- **`(the option list after 182 commands)`** — 16 per-event expand/collapse options: "collapse the being born" … "expand the forest fire"
  - reach it: the acceptance script's first 182 commands (… "map the sparking of the tinder to the dying unexpectedly", "map the sparking of the tinder to the adjusting of his words"), the option list there
- **`(the option list after 183 commands)`** — 16 per-event expand/collapse options: "collapse the being born" … "expand the forest fire"
  - reach it: the acceptance script's first 183 commands (… "map the sparking of the tinder to the adjusting of his words", "map the spreading of the ember to the adjusting of his words"), the option list there
- **`(the option list after 184 commands)`** — 16 per-event expand/collapse options: "collapse the being born" … "expand the forest fire"
  - reach it: the acceptance script's first 184 commands (… "map the spreading of the ember to the adjusting of his words", "map the spreading of the flame to the spreading across the land"), the option list there
- **`(the option list after 185 commands)`** — 16 per-event expand/collapse options: "collapse the being born" … "expand the forest fire"
  - reach it: the acceptance script's first 185 commands (… "map the spreading of the flame to the spreading across the land", "map the consumption of all to the being read and repeated and reprinted"), the option list there
- **`(the option list after 186 commands)`** — 16 per-event expand/collapse options: "collapse the being born" … "expand the forest fire"
  - reach it: the acceptance script's first 186 commands (… "map the consumption of all to the being read and repeated and reprinted", "map the ash left behind to the passing of time, in the wise man's story"), the option list there
- **`(the option list after 187 commands)`** — 16 per-event expand/collapse options: "collapse the being born" … "expand the forest fire"
  - reach it: the acceptance script's first 187 commands (… "map the ash left behind to the passing of time, in the wise man's story", "apply the Voice of Fire"), the option list there
- **`(the option list after 188 commands)`** — 16 per-event expand/collapse options: "collapse the being born" … "expand the forest fire"
  - reach it: the acceptance script's first 188 commands (… "apply the Voice of Fire", "collapse the unmapped"), the option list there
- **`(the option list after 189 commands)`** — 16 per-event expand/collapse options: "collapse the being born" … "expand the forest fire"
  - reach it: the acceptance script's first 189 commands (… "collapse the unmapped", "object that there is no fire"), the option list there
- **`(the option list after 190 commands)`** — 16 per-event expand/collapse options: "collapse the being born" … "expand the forest fire"
  - reach it: the acceptance script's first 190 commands (… "object that there is no fire", "set aside the second solution"), the option list there
- **`(the option list after 191 commands)`** — 16 per-event expand/collapse options: "collapse the being born" … "expand the forest fire"
  - reach it: the acceptance script's first 191 commands (… "set aside the second solution", "map the sparking of the tinder to the dying unexpectedly"), the option list there
- **`(the option list after 192 commands)`** — 16 per-event expand/collapse options: "collapse the being born" … "expand the forest fire"
  - reach it: the acceptance script's first 192 commands (… "map the sparking of the tinder to the dying unexpectedly", "resume the first solution"), the option list there
- **`(the option list after 193 commands)`** — 16 per-event expand/collapse options: "collapse the being born" … "expand the forest fire"
  - reach it: the acceptance script's first 193 commands (… "resume the first solution", "set aside the first solution"), the option list there
- **`(the option list after 194 commands)`** — 16 per-event expand/collapse options: "collapse the being born" … "expand the forest fire"
  - reach it: the acceptance script's first 194 commands (… "set aside the first solution", "map the sparking of the tinder to the adjusting of his words"), the option list there
- **`(the option list after 195 commands)`** — 16 per-event expand/collapse options: "collapse the being born" … "expand the forest fire"
  - reach it: the acceptance script's first 195 commands (… "map the sparking of the tinder to the adjusting of his words", "resume the second solution"), the option list there
- **`(the option list after 196 commands)`** — 16 per-event expand/collapse options: "collapse the being born" … "expand the forest fire"
  - reach it: the acceptance script's first 196 commands (… "resume the second solution", "object that the fireplace is too abstract"), the option list there
- **`(the option list after 197 commands)`** — 16 per-event expand/collapse options: "collapse the being born" … "expand the forest fire"
  - reach it: the acceptance script's first 197 commands (… "object that the fireplace is too abstract", "object that the spark is the myth, not the death"), the option list there
- **`(the option list after 198 commands)`** — 16 per-event expand/collapse options: "collapse the being born" … "expand the forest fire"
  - reach it: the acceptance script's first 198 commands (… "object that the spark is the myth, not the death", "object that the ash is still structured"), the option list there
- **`(the option list after 199 commands)`** — 16 per-event expand/collapse options: "collapse the being born" … "expand the forest fire"
  - reach it: the acceptance script's first 199 commands (… "object that the ash is still structured", "remember the wise man's story"), the option list there
- **`(the option list after 200 commands)`** — 16 per-event expand/collapse options: "collapse the being born" … "expand the forest fire"
  - reach it: the acceptance script's first 200 commands (… "remember the wise man's story", "say Ok, I guess"), the option list there
- **`(the option list after 201 commands)`** — 16 per-event expand/collapse options: "collapse the being born" … "expand the forest fire"
  - reach it: the acceptance script's first 201 commands (… "say Ok, I guess", "remember the wise man's story"), the option list there
- **`(the option list after 202 commands)`** — 16 per-event expand/collapse options: "collapse the being born" … "expand the forest fire"
  - reach it: the acceptance script's first 202 commands (… "remember the wise man's story", "remember the saying of Ok, I guess"), the option list there
- **`(the option list after 203 commands)`** — 16 per-event expand/collapse options: "collapse the being born" … "expand the forest fire"
  - reach it: the acceptance script's first 203 commands (… "remember the saying of Ok, I guess", "remember the second listening"), the option list there
- **`(the option list after 204 commands)`** — 16 per-event expand/collapse options: "collapse the being born" … "expand the forest fire"
  - reach it: the acceptance script's first 204 commands (… "remember the second listening", "remember the ember"), the option list there
- **`(the option list after 205 commands)`** — 16 per-event expand/collapse options: "collapse the being born" … "expand the forest fire"
  - reach it: the acceptance script's first 205 commands (… "remember the ember", "remember the lighting of the pyre"), the option list there
- **`(the option list after 206 commands)`** — 14 per-event expand/collapse options: "collapse the packing" … "expand the forest fire"
  - reach it: the acceptance script's first 206 commands (… "remember the lighting of the pyre", "try the Pillaging on the house in the woods"), the option list there
- **`(the option list after 207 commands)`** — 14 per-event expand/collapse options: "collapse the packing" … "expand the forest fire"
  - reach it: the acceptance script's first 207 commands (… "try the Pillaging on the house in the woods", "map the living in their home to the moving in"), the option list there
- **`(the option list after 208 commands)`** — 14 per-event expand/collapse options: "collapse the packing" … "expand the forest fire"
  - reach it: the acceptance script's first 208 commands (… "map the living in their home to the moving in", "map the entering of their home to the lighting of the rag"), the option list there
- **`(the option list after 209 commands)`** — 14 per-event expand/collapse options: "collapse the packing" … "expand the forest fire"
  - reach it: the acceptance script's first 209 commands (… "map the entering of their home to the lighting of the rag", "map the taking of things from their home to the hurling of the rag onto the roof"), the option list there
- **`(the option list after 210 commands)`** — 14 per-event expand/collapse options: "collapse the packing" … "expand the forest fire"
  - reach it: the acceptance script's first 210 commands (… "map the taking of things from their home to the hurling of the rag onto the roof", "map the entering of their home to the scattering"), the option list there
- **`(the option list after 211 commands)`** — 14 per-event expand/collapse options: "collapse the packing" … "expand the forest fire"
  - reach it: the acceptance script's first 211 commands (… "map the entering of their home to the scattering", "remember the tinder"), the option list there
- **`(the option list after 212 commands)`** — 16 per-event expand/collapse options: "collapse the being born" … "expand the forest fire"
  - reach it: the acceptance script's first 212 commands (… "remember the tinder", "put down the chalk"), the option list there
- **`(the option list after 213 commands)`** — 16 per-event expand/collapse options: "collapse the being born" … "expand the forest fire"
  - reach it: the acceptance script's first 213 commands (… "put down the chalk", "remember the Pillaging"), the option list there

Scanned in 726 s.
