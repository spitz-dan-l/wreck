# Orchestrator's own reading of the B1 headless transcript

Observations to fold into Phase B3 alongside critique 4.

1. **`follows` paragraphs are doubled on reprint.** In `remember the campfire
   story`, "The fire starts, spreading first…" prints under `light a match`
   *and* under `touch the flame to the tinder`; "The remaining embers fizzle
   out…" prints under the second `sing` *and* under `sleep in tents`. The
   knowledge copy attaches a consequence-only ¶ to two events. Must fix.
2. **The Fire's rendition repeats an absorbed event's consequence once per
   step.** Steps 4, 5, 6 on the campfire all reprint "The tinder burns
   quickly… The fire starts…" three times; the wise man's 4 and 5 both
   reprint ¶12. Print the target consequence once for a run of steps that
   share an event (the step commands still each get their own line).
3. **Annotations doubled on `remember <event>`.** "— the ember / — the flame
   / — the blaze" then "— the ember, in the Voice of Fire" ×3. One form.
4. **Objections after the first lose their quotation marks.** "There's no
   fire, no wood, no burning directly involved." prints bare; the .md has it
   as speech. Quote every character sentence as the .md does.
5. **Authored consequences drift in person.** "You grow in number. Word of
   his wisdom spreads, and more of you come." (the followers' voice, then
   "his"). Sweep every *(author)* consequence for a consistent second person
   within its voice.
6. **`speak as` prints nothing in text mode.** Acceptable once the board
   draws the bar (B2), but the headless transcript should at least show
   the bar's text ("— the children —") so play.js output is legible.
7. **`map` prints nothing on success.** Same: fine once badges exist; in
   text mode print the reference line ("→ the laying of the tinder in the
   pit").
8. The Fire's rendition on the campfire reads well; the wise man's
   figurative rendition reads well and is the payoff. Keep.
